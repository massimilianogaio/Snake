#pragma once
//#include <glad/glad.h>
//#include <GLFW/glfw3.h>
//
//
#include "../shader/Shader.h"
#include "QuadTransform.h"
//using namespace glm;
//
//
enum Direction {
    None,
    Up,
    Down,
    Left,
    Right
};
//void framebuffer_size_callback(GLFWwindow* window, int width, int height);
//int main();
//void DrawQuad(Shader& shader, mat4& projectionMatrix, QuadTransform quadData);
//void processInput(GLFWwindow* window);
//void CreateSnakeQuad(vec3 position, vec3 rotation, vec3 scale, vec4 color);
//void MoveSnakeTorwardDirection(Direction direction);
//int GetRandomNumber(int minRange, int maxRange);
//void SetPointGoalRandomPosition();
//bool IsPointGoalReached();